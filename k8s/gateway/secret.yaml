# Local lab credentials only. Not encrypted; do not reuse in any real environment.
apiVersion: v1
kind: Secret
metadata:
  name: gateway-secrets
  namespace: gateway-lab
type: Opaque
stringData:
  GATEWAY_API_KEYS: sk-lab:alice
  MOCK_PROVIDER_API_KEY: mock-secret
